# 6. 게시글 수정하기

# 6.1 BoardService

> **게시글 수정 로직은 2장에서 적용한 게시글수정 메서드의 `Optional` 처리 내용을 제외하면 추가로 변경된 부분은 없습니다.**
> 

**BoardService.java**

```jsx
@Transactional
public void 게시글수정(Integer id, BoardRequest.UpdateDTO requestDTO) {
    Board board = boardRepository.findById(id);
    // 더티 체킹
    board.setTitle(requestDTO.getTitle());
    board.setContent(requestDTO.getContent());
} // 트랜잭션 종료시 flush()
```

# 6.2 **BoardRepositoryTest**

`update_test`메서드의 `findById`의 반환 값에 `get()`을 사용해 처리합니다.

**BoardRepositoryTest.java**

```java
@Test
public void update_test() {
    // given
    int id = 2;
    // when
    Board board = boardRepository.findById(id).get();
    board.setTitle("title-update");
    board.setContent("Update-test");
    em.flush();
    // eye
    Board result = boardRepository.findById(id).get();
    System.out.println("=======================");
    System.out.println("Board title : " + result.getTitle());
    System.out.println("Board content : " + result.getContent());
}

```