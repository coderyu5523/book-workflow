# 8. 게시글 쓰기 리팩토링

게시글 쓰기에 대한 인증, 권한 체크를 하겠습니다.

# 8.1 Controller 인증 체크

우선 게시글 쓰기 페이지(saveForm)를 접근할 때 로그인 체크를 합니다.

**BoardController.java**

```java
@GetMapping("/boards/save-form")
public String saveForm() {
    User sessionUser = (User)session.getAttribute("session");
    if(sessionUser == null){
        throw new Exception401("로그인이 필요합니다.");
    }
    return "board/save-form";
}
```

그리고 게시글 쓰기(save) 메서드에서 세션 정보를 BoardService로 전달합니다.

**BoardController.java**

```java
@PostMapping("/boards/save")
public String save(BoardRequest.SaveDTO requestDTO){
    User sessionUser = (User)session.getAttribute("session");
    if(sessionUser == null){
        throw new Exception401("로그인이 필요합니다.");
    }
    boardService.게시글추가(requestDTO,sessionUser);
    return "redirect:/";
}
```

# 8.2 RequestDTO 수정

BoardRequest의 SaveDTO 클래스에 user 필드를 추가합니다.

**BoardRequest.java**

```java
package com.metacoding.springv1.board;

import com.metacoding.springv1.user.User;
import lombok.Data;

public class BoardRequest {

    @Data
    public static class SaveDTO {

        private String title;
        private String content;
        private User user;  // user 필드 추가

        // 빌더 패턴으로 엔티티 생성
        public Board toEntity(User user) {
            return Board.builder()
                .title(title)
                .content(content)
                .user(user)  
                .build();
        }
    }
    //중략
}
```

# 8.3 Service에서 게시글 쓰기 수정

BoardService의 게시글 추가 메서드는 DTO와 세션 정보(SessionUser)를 전달받고, DTO의 toEntity에 세션 정보를 주입해 User 연관이 설정된 Board 엔티티를 생성합니다.

**BoardService.java**

```java
package com.metacoding.springv1.board;

import java.util.List;
import java.util.stream.Collectors;
import org.springframework.stereotype.Service;
import jakarta.transaction.Transactional;
import lombok.RequiredArgsConstructor;
import com.metacoding.springv1.core.handler.ex.*;
import com.metacoding.springv1.user.User;

@RequiredArgsConstructor
@Service
public class BoardService {

    private final BoardRepository boardRepository;

    @Transactional
    public void 게시글추가(BoardRequest.SaveDTO requestDTO, User sessionUser){
        Board board = requestDTO.toEntity(sessionUser); //DTO -> 엔티티
        boardRepository.save(board);
    }
    // 중략
}
```

사용자 cos 계정으로 로그인한 뒤 게시글을 작성합니다.

![**1.png**](8%20%EA%B2%8C%EC%8B%9C%EA%B8%80%20%EC%93%B0%EA%B8%B0%20%EB%A6%AC%ED%8C%A9%ED%86%A0%EB%A7%81/image.png)

**1.png**

게시글 상세보기 페이지와 h2 콘솔에서 게시글에 사용자 정보가 포함되어 저장된 것을 확인할 수 있습니다.

![**2.png**](8%20%EA%B2%8C%EC%8B%9C%EA%B8%80%20%EC%93%B0%EA%B8%B0%20%EB%A6%AC%ED%8C%A9%ED%86%A0%EB%A7%81/image%201.png)

**2.png**

![**3.png**](8%20%EA%B2%8C%EC%8B%9C%EA%B8%80%20%EC%93%B0%EA%B8%B0%20%EB%A6%AC%ED%8C%A9%ED%86%A0%EB%A7%81/image%202.png)

**3.png**