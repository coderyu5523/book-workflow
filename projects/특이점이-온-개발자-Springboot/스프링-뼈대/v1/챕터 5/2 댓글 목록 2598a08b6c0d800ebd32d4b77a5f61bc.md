# 2. 댓글 목록

# 2.1 findByIdJoinUserAndReply()

findByIdJoinUserAndReply 메서드를 생성합니다. 

> **INNER JOIN은 매칭되는 행만 반환하므로 댓글이 없는 게시글은 결과에서 제외됩니다.
모든 게시글을 조회하려면 LEFT(RIGHT) OUTER JOIN을 사용해야 합니다.**
> 

**BoardRepository.java**

```java
public Optional<Board> findByIdJoinUserAndReply(int id){
    Optional<Board> board = em.createQuery("select b from Board b join fetch b.user left join fetch b.replies where b.id = :id", Board.class)
                        .setParameter("id", id)
                        .getResultStream()
                        .findFirst();
    return board;
}
```

# 2.2 단위 테스트

findByIdJoinUserAndReply_test 메서드를 구현한 뒤 실행하여, 결과를 확인합니다. 검증은 ID가 2인 댓글을 대상으로 수행합니다.

**BoardRepositoryTest.java**

```java
@Test
public void findByIdJoinUserAndReply_test(){
    //given
    int id = 1;
    //when
    Board board = boardRepository.findByIdJoinUserAndReply(id).get();
    //eye
    System.out.println("=======================");
    System.out.println("Board ID : " + board.getId());
    System.out.println("username : " + board.getUser().getUsername());
    System.out.println("Reply : " + board.getReplies().get(1).getComment());
   }
```

실행된 쿼리를 보면 user 엔티티와 reply 엔티티를 함께 가져오는 것을 확인할 수 있습니다.

![**1.png**](2%20%EB%8C%93%EA%B8%80%20%EB%AA%A9%EB%A1%9D/image.png)

**1.png**

# 2.3 Service

BoardService의 게시글상세 메서드에서 기존 findByIdJoinUser 호출을 findByIdJoinUserAndReply로 대체합니다.

**BoardService.java**

```java
public BoardResponse.DetailDTO 게시글상세(Integer id) {
    //Board board = boardRepository.findById(id).get(); // pk 로 조회
    //Board board = boardRepository.findByIdJoinUser(id).orElseThrow(() -> new Exception404("게시글을 찾을 수 없습니다."));
    Board board = boardRepository.findByIdJoinUserAndReply(id).orElseThrow(() -> new Exception404("게시글을 찾을 수 없습니다."));
    BoardResponse.DetailDTO dto = new BoardResponse.DetailDTO(board); // DTO 담기기
    return dto;
}
```

# 2.4 ResponseDTO

BoardResponse의 DetailDTO를 다음과 같이 수정합니다.

> **List 타입 필드는 DTO 안에서 또 다른 DTO 객체를 생성해, 엔티티를 하나씩 변환하여 담습니다.**
> 

**BoardResponse.java**

```java
package com.metacoding.springv1.board;

import java.util.*;
import java.util.stream.Collectors;
import lombok.Data;
import com.metacoding.springv1.reply.Reply;

public class BoardResponse {

    @Data
    public static class DetailDTO {

        private Integer id;
        private String title;
        private String content;
        private Integer userId;
        private String username;
        private List<ReplyDTO> replies = new ArrayList<>();

        public DetailDTO(Board board) {
            this.id = board.getId();
            this.title = board.getTitle();
            this.content = board.getContent();
            this.userId = board.getUser().getId();
            this.username = board.getUser().getUsername();
            this.replies = board.getReplies().stream()
                                .map(reply -> new ReplyDTO(reply))
                                .toList();
        }
        // List 타입의 댓글을 담는 DTO                                        
        @Data
        public class ReplyDTO {
            private Integer id;
            private String username;
            private String comment;

            public ReplyDTO(Reply reply) {
                this.id = reply.getId();
                this.username = reply.getUser().getUsername();
                this.comment = reply.getComment();
            }
        }
    }
    // DTO 중략
}
```

# 2.5 detail.mustache

detail.mustache 로 이동하면, 아래와 같이 댓글 영역을 확인할 수 있습니다.

![**2.png**](2%20%EB%8C%93%EA%B8%80%20%EB%AA%A9%EB%A1%9D/image%201.png)

**2.png**

컨트롤러에서 전달받은 model 객체에서 List 필드를 꺼내어, 화면의 각 영역에 데이터를 바인딩합니다.

**detail.mustache**

```java
<div class="list-group">
   <!-- 댓글아이템 -->
   {{#model.replies}}
   <div class="list-group-item d-flex justify-content-between align-items-center">
        <div class="d-flex">
            <div class="px-1 me-1 bg-secondary text-white rounded">{{username}}</div>
            <div>{{comment}}</div>
        </div>
        <form action="/replies/{{id}}/delete" method="post">
           <button class="btn">🗑</button>
        </form>
    </div>
    {/model.replies}}
</div>
```

프로젝트 실행 후 게시글 상세보기 페이지에서 댓글 목록을 확인할 수 있습니다.

![**3.png**](2%20%EB%8C%93%EA%B8%80%20%EB%AA%A9%EB%A1%9D/image%202.png)

**3.png**