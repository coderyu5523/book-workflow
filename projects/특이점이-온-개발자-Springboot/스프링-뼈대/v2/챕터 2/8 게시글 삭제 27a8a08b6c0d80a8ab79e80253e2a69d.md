# 8. 게시글 삭제

# 8.1 BoardService

BoardService에 게시글 삭제 메서드를 구현합니다. JpaRepository에서 제공하는 deleteById를 사용하면 게시글을 간단하게 삭제할 수 있습니다.

**BoardService.java**

```java
@Transactional
public void 게시글삭제(Integer boardId, Integer sessionUserId) {
    Board findBoard = boardRepository.findById(boardId).orElseThrow(() -> new Exception404("게시글을 찾을 수 없습니다"));
    if (!findBoard.getUser().getId().equals(sessionUserId))
        throw new Exception403("게시글을 삭제할 권한이 없습니다");
    boardRepository.deleteById(boardId);
}
```

# 8.2 BoardController

BoardController에 게시글 삭제 메서드를 구현합니다.
@DeleteMapping을 사용하며, 삭제가 성공하면 응답 바디는 null 값을 반환합니다.

**BoardController.java**

```java
@DeleteMapping("/{boardId}")
public ResponseEntity<?> deleteById(@AuthenticationPrincipal User sessionUser,
        @PathVariable("boardId") Integer boardId) {
    boardService.게시글삭제(boardId, sessionUser.getId());
    return Resp.ok(null);
}
```

API 요청 시 게시글이 삭제된 것을 확인할 수 있습니다.

![**1.png**](8%20%EA%B2%8C%EC%8B%9C%EA%B8%80%20%EC%82%AD%EC%A0%9C/image.png)

**1.png**