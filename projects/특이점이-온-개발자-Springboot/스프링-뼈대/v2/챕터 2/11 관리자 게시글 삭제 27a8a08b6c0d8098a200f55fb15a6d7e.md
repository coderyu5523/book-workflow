# 11. 관리자 게시글 삭제

11장과 12장은 관리자 권한을 가진 사용자의 기능을 구현해보겠습니다.

# 11.1 관리자 설정

SecurityConfig의 필터 체인에는 관리자 전용 설정이 포함되어 있습니다. /api/admin으로 시작하는 요청은 ADMIN 권한을 가진 사용자만 접근할 수 있도록 제한합니다.

**core > config > SecurityConfig.java**

![image.png](11%20%EA%B4%80%EB%A6%AC%EC%9E%90%20%EA%B2%8C%EC%8B%9C%EA%B8%80%20%EC%82%AD%EC%A0%9C/image.png)

그리고 User 더미 데이터 중 2번 회원은 **cos**의 역할이 USER, ADMIN으로 관리자 역할을 가지고 있습니다.

**db > data.sql**

![**1.png**](11%20%EA%B4%80%EB%A6%AC%EC%9E%90%20%EA%B2%8C%EC%8B%9C%EA%B8%80%20%EC%82%AD%EC%A0%9C/image%201.png)

**1.png**

# 11.2 AdminService

AdminService를 다음과 같이 구현합니다. 관리자_게시글삭제 메서드는 별도의 권한 검증을 하지 않습니다. 이미 ADMIN 권한 사용자만 접근할 수 있도록 보안 설정이 적용되어 있기 때문입니다.

**AdminService.java**

```java
package com.metacoding.springv2.admin;

import org.springframework.stereotype.Service;
import com.metacoding.springv2.board.*;
import com.metacoding.springv2.core.handler.ex.*;
import com.metacoding.springv2.auth.*;
import com.metacoding.springv2.user.*;
import lombok.RequiredArgsConstructor;
import org.springframework.transaction.annotation.Transactional;
import java.util.*;

@Service
@RequiredArgsConstructor
public class AdminService {
    private final BoardRepository boardRepository;
    private final UserRepository userRepository;

    @Transactional
    public void 관리자_게시글삭제(Integer boardId) {
        boardRepository.findById(boardId)
                .orElseThrow(() -> new Exception404("게시글을 찾을 수 없습니다"));
        boardRepository.deleteById(boardId);
    }
}
```

# 11.3 AdminController

AdminController는 공통 URL 경로를 **/api/admin**으로 설정합니다. 따라서 이 컨트롤러는 ADMIN 권한을 가진 사용자만 접근할 수 있습니다.

**AdminController.java**

```java
package com.metacoding.springv2.admin;

import com.metacoding.springv2.core.util.Resp;
import lombok.RequiredArgsConstructor;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;
import org.springframework.validation.Errors;
import jakarta.validation.Valid;
import com.metacoding.springv2.user.*;

@RequestMapping("/api/admin")
@RequiredArgsConstructor
@RestController
public class AdminController {
    private final AdminService adminService;

    @DeleteMapping("/boards/{boardId}")
    public ResponseEntity<?> deleteBoard(@PathVariable("boardId") Integer boardId) {
        adminService.관리자_게시글삭제(boardId);
        return Resp.ok(null);
    }
}
```

User 역할인 **ssar** 사용자는 /api/admin 으로 시작하는 URL에 접근할 수 없습니다.

![**2.png**](11%20%EA%B4%80%EB%A6%AC%EC%9E%90%20%EA%B2%8C%EC%8B%9C%EA%B8%80%20%EC%82%AD%EC%A0%9C/image%202.png)

**2.png**

이번에는 ADMIN 역할인 **cos** 사용자로 로그인합니다.

![**3.png**](11%20%EA%B4%80%EB%A6%AC%EC%9E%90%20%EA%B2%8C%EC%8B%9C%EA%B8%80%20%EC%82%AD%EC%A0%9C/image%203.png)

**3.png**

**cos** 의 JWT를 인증에 포함해 API 요청을 보내면 정상적으로 삭제가 수행됩니다. 1번 게시글의 작성자는 **ssar**이지만, 관리자 권한을 가지고 있으므로 삭제가 가능합니다.

![**4.png**](11%20%EA%B4%80%EB%A6%AC%EC%9E%90%20%EA%B2%8C%EC%8B%9C%EA%B8%80%20%EC%82%AD%EC%A0%9C/image%204.png)

**4.png**