# 6. 게시글 수정 정보

이번 장에서는 게시글 수정을 위해 필요한 정보를 조회합니다. 사용자가 게시글 수정 페이지로 이동했을 때, 기존 게시글의 내용을 불러와 화면에 보여주는 기능입니다.

## 6.1 BoardService

BoardService에 게시글정보 메서드를 구현합니다. JpaRepository의 findById를 활용해 게시글 정보를 조회합니다.

**BoardService.java**

```java
public BoardResponse.DTO 게시글정보(Integer boardId, Integer sessionUserId) {
    Board findBoard = boardRepository.findById(boardId)
            .orElseThrow(() -> new Exception404("게시글을 찾을 수 없습니다"));
    if (!findBoard.getUser().getId().equals(sessionUserId))
        throw new Exception403("게시글에 접근할 권한이 없습니다");
    return new BoardResponse.DTO(findBoard);
}
```

# 6.2 BoardController

그리고 BoardController에 updateInfo 메서드를 구현합니다. 

**BoardController.java**

```java
@GetMapping("/{boardId}/edit")
public ResponseEntity<?> updateInfo(@AuthenticationPrincipal User sessionUser,
        @PathVariable("boardId") Integer boardId) {
    var responseDTO = boardService.게시글정보(boardId,sessionUser.getId());
    return Resp.ok(responseDTO);
}
```

유저네임이 ssar인 회원의 JWT를 사용해 API 요청을 하면 정상적으로 게시글정보가 조회됩니다.

![**1.png**](6%20%EA%B2%8C%EC%8B%9C%EA%B8%80%20%EC%88%98%EC%A0%95%20%EC%A0%95%EB%B3%B4/image.png)

**1.png**

그리고 작성자가 다른 게시글은 403 에러가 발생합니다.

![**2.png**](6%20%EA%B2%8C%EC%8B%9C%EA%B8%80%20%EC%88%98%EC%A0%95%20%EC%A0%95%EB%B3%B4/image%201.png)

**2.png**