# 5. 관리자 통합 테스트

AdminControllerTest를 다음과 같이 작성합니다. 관리자 테스트는 ADMIN 역할을 가진 cos 사용자를 사용해 JWT를 발급해 검증에 사용합니다.

**controller/AdminControllerTest.java**

```jsx
package com.metacoding.springv2.controller;

import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;
import org.junit.jupiter.api.*;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.web.servlet.ResultActions;
import org.springframework.test.web.servlet.request.MockMvcRequestBuilders;
import org.springframework.test.web.servlet.result.MockMvcResultHandlers;
import org.springframework.transaction.annotation.Transactional;
import org.springframework.beans.factory.annotation.Autowired;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.metacoding.springv2.user.*;
import com.metacoding.springv2.MyRestDoc;
import com.metacoding.springv2.core.util.JwtUtil;
import org.springframework.http.MediaType;

@Transactional
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.MOCK)
public class AdminControllerTest extends MyRestDoc {

        @Autowired
        private ObjectMapper om;

        private String adminToken;

        @BeforeEach
        void setUp() {
                // 테스트용 사용자 생성 및 JWT 토큰 생성
                User testUser = User.builder()
                                .id(1)
                                .username("ssar")
                                .password("1234")
                                .email("ssar@metacoding.com")
                                .roles("USER")
                                .build();
                adminToken = JwtUtil.create(testUser);
        }   
    // 메서드 작성
}
```

# 5.1 관리자 게시글 삭제 성공 테스트

관리자 게시글 삭제를 위한 프롬프트를 다음과 같이 작성합니다.

> **역할 : 너는 스프링 개발자야**
**맥락 : 지금 AdminControllerTest에 관리자 게시글 삭제 통합 테스트를 진행 중이야.**
**지시 사항 :  boardId 가 2번인 게시글에 대한 게시글 삭제 성공 케이스 작성해줘
제약 : given 데이터는 @BeforeEach, 더미 데이터는 data.sql, then 검증 시 andDo 사용해야 해.**
**결과 형식 : 형식은  given, when, then 형태로 작성해줘**
> 

프롬프트를 입력하면 테스트 코드가 완성됩니다.

![**1.png**](5%20%EA%B4%80%EB%A6%AC%EC%9E%90%20%ED%86%B5%ED%95%A9%20%ED%85%8C%EC%8A%A4%ED%8A%B8/image.png)

**1.png**

deleteById_success_test는 2번 게시글을 삭제한 뒤 응답 결과를 검증합니다. 해당 게시글의 작성자는 ssar 사용자이지만, 관리자 권한으로 삭제 테스트를 수행합니다.

**controller/AdminControllerTest.java**

```jsx
@Test
public void deleteById_success_test() throws Exception {
        // given
        Integer boardId = 2;
        // when
        ResultActions result = mvc.perform(
                        MockMvcRequestBuilders.delete("/api/admin/boards/" + boardId)
                                        .header("Authorization", adminToken));
        // then
        result.andExpect(status().isOk())
                        .andExpect(jsonPath("$.status").value(200))
                        .andExpect(jsonPath("$.msg").value("성공"))
                        .andExpect(jsonPath("$.body").isEmpty())
                        .andDo(MockMvcResultHandlers.print()).andDo(document);
}
```

테스트가 정상적으로 실행되었습니다.

![**2.png**](5%20%EA%B4%80%EB%A6%AC%EC%9E%90%20%ED%86%B5%ED%95%A9%20%ED%85%8C%EC%8A%A4%ED%8A%B8/image%201.png)

**2.png**

# 5.2 관리자 역할 수정 성공 테스트

관리자 역할 수정을 위해 다음과 같이 프롬프트를 작성합니다.

> **역할 : 너는 스프링 개발자야**
**맥락 : 지금 AdminControllerTest에 관리자 역할 수정통합 테스트를 진행 중이야.**
**지시 사항 :  userId 가 1번인 회원의 역할을 USER, ADMIN로 수정 성공 케이스 작성해줘
제약 : given 데이터는 @BeforeEach, 더미 데이터는 data.sql, then 검증 시 andDo 사용해야 해.**
**결과 형식 : 형식은  given, when, then 형태로 작성해줘**
> 

테스트 코드가 완성되었습니다.

![**3.png**](5%20%EA%B4%80%EB%A6%AC%EC%9E%90%20%ED%86%B5%ED%95%A9%20%ED%85%8C%EC%8A%A4%ED%8A%B8/image%202.png)

**3.png**

rolesUpdate_success_test 메서드는 ssar 사용자의 역할에 ADMIN을 추가 후, 응답 데이터를 검증합니다.

**controller/AdminControllerTest.java**

```jsx
@Test
public void rolesUpdate_success_test() throws Exception {
       // given
       Integer userId = 1;
       UserRequest.RolesDTO rolesDTO = new UserRequest.RolesDTO("USER,ADMIN");
       String requestBody = om.writeValueAsString(rolesDTO);
       // when
       ResultActions result = mvc.perform(
                       MockMvcRequestBuilders.put("/api/admin/users/" + userId)
                                       .header("Authorization", adminToken)
                                       .contentType(MediaType.APPLICATION_JSON)
                                       .content(requestBody));
       // then
       result.andExpect(status().isOk())
                       .andExpect(jsonPath("$.status").value(200))
                       .andExpect(jsonPath("$.msg").value("성공"))
                       .andExpect(jsonPath("$.body.id").value(1))
                       .andExpect(jsonPath("$.body.username").value("ssar"))
                       .andExpect(jsonPath("$.body.email").value("ssar@metacoding.com"))
                       .andExpect(jsonPath("$.body.roles").value("USER,ADMIN"))
                       .andDo(MockMvcResultHandlers.print()).andDo(document);
}
```

테스트가 정상적으로 실행되며, 응답 데이터에서 역할이 변경된 것을 확인할 수 있습니다.

![**4.png**](5%20%EA%B4%80%EB%A6%AC%EC%9E%90%20%ED%86%B5%ED%95%A9%20%ED%85%8C%EC%8A%A4%ED%8A%B8/image%203.png)

**4.png**