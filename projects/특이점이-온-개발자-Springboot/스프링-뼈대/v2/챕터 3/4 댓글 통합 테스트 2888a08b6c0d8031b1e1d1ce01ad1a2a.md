# 4. 댓글 통합 테스트

ReplyControllerTest 클래스를 다음과 같이 작성합니다. 

**controller/ReplyControllerTest** 

```jsx
package com.metacoding.springv2.controller;

import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;
import org.junit.jupiter.api.*;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.ResultActions;
import org.springframework.test.web.servlet.request.MockMvcRequestBuilders;
import org.springframework.test.web.servlet.result.MockMvcResultHandlers;
import org.springframework.transaction.annotation.Transactional;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.metacoding.springv2.MyRestDoc;
import com.metacoding.springv2.core.util.JwtUtil;
import com.metacoding.springv2.reply.ReplyRequest;
import com.metacoding.springv2.user.User;

@Transactional
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.MOCK)
public class ReplyControllerTest extends MyRestDoc {
        @Autowired
        private ObjectMapper om;

        private String accessToken;

        @BeforeEach
        void setUp() {
                // 테스트용 사용자 생성 및 JWT 토큰 생성
                User testUser = User.builder()
                                .id(1)
                                .username("ssar")
                                .password("1234")
                                .email("ssar@metacoding.com")
                                .roles("USER")
                                .build();
                accessToken = JwtUtil.create(testUser);
        }
			// 메서드 작성
}
```

# 4.1 댓글 작성 성공 테스트

댓글 작성 성공 테스트를 위해 다음과 같이 프롬프트를 작성 후 입력합니다.

> **역할 : 너는 스프링 개발자야**
**맥락 : 지금 ReplyControllerTest에 댓글 작성 통합 테스트를 진행 중이야.**
**지시 사항 :  save 성공 케이스 작성해줘
제약 : given 데이터는 @BeforeEach, 더미 데이터는 data.sql, then 검증 시 andDo 사용해야 해.**
**결과 형식 : 형식은  given, when, then 형태로 작성해줘**
> 

테스트 코드가 완성되었습니다.

![**1.png**](4%20%EB%8C%93%EA%B8%80%20%ED%86%B5%ED%95%A9%20%ED%85%8C%EC%8A%A4%ED%8A%B8/image.png)

**1.png**

save_success_test 메서드는 SaveDTO를 사용해 API 요청 후 응답 데이터를 검증합니다.

**controller/ReplyControllerTest** 

```jsx
@Test
public void save_success_test() throws Exception {
        // given
        ReplyRequest.SaveDTO saveDTO = new ReplyRequest.SaveDTO("test-comment", 1);
        String requestBody = om.writeValueAsString(saveDTO);
        // when
        ResultActions result = mvc.perform(
                        MockMvcRequestBuilders.post("/api/replies")
                                        .header("Authorization", accessToken)
                                        .contentType(MediaType.APPLICATION_JSON)
                                        .content(requestBody));
        // then
        result.andExpect(status().isOk())
                        .andExpect(jsonPath("$.status").value(200))
                        .andExpect(jsonPath("$.msg").value("성공"))
                        .andExpect(jsonPath("$.body.id").value(6))
                        .andExpect(jsonPath("$.body.comment").value("test-comment"))
                        .andExpect(jsonPath("$.body.userId").value(1))
                        .andExpect(jsonPath("$.body.boardId").value(1))
                        .andExpect(jsonPath("$.body.username").value("ssar"))
                        .andExpect(jsonPath("$.body.isOwner").value(true))
                        .andDo(MockMvcResultHandlers.print()).andDo(document);
}
```

테스트가 정상적으로 실행된 것을 확인할 수 있습니다.

![**2.png**](4%20%EB%8C%93%EA%B8%80%20%ED%86%B5%ED%95%A9%20%ED%85%8C%EC%8A%A4%ED%8A%B8/image%201.png)

**2.png**

# 4.2 댓글 삭제 성공 테스트

다음으로 댓글 삭제 성공 테스트를 위한 프롬프트를 작성합니다.

> **역할 : 너는 스프링 개발자야**
**맥락 : 지금 ReplyControllerTest에 댓글 삭제 통합 테스트를 진행 중이야.**
**지시 사항 :  replyId가 4번인 댓글에 대한 deleteById 성공 케이스 작성해줘
제약 : given 데이터는 @BeforeEach, 더미 데이터는 data.sql, then 검증 시 andDo 사용해야 해.**
**결과 형식 : 형식은  given, when, then 형태로 작성해줘**
> 

테스트 코드가 완성되었습니다.

![**3.png**](4%20%EB%8C%93%EA%B8%80%20%ED%86%B5%ED%95%A9%20%ED%85%8C%EC%8A%A4%ED%8A%B8/image%202.png)

**3.png**

deleteById_success_test 메서드는 4번 댓글 삭제 후 상태 코드와 Body 데이터가 NULL인지 검증합니다.

**controller/ReplyControllerTest** 

```jsx
@Test
public void deleteById_success_test() throws Exception {
        // given
        Integer replyId = 4;
        // when
        ResultActions result = mvc.perform(
                        MockMvcRequestBuilders.delete("/api/replies/" + replyId)
                                        .header("Authorization", accessToken));
        // then
        result.andExpect(status().isOk())
                        .andExpect(jsonPath("$.status").value(200))
                        .andExpect(jsonPath("$.msg").value("성공"))
                        .andExpect(jsonPath("$.body").isEmpty())
                        .andDo(MockMvcResultHandlers.print()).andDo(document);
}
```

테스트가 성공적으로 완료되었습니다.

![**4.png**](4%20%EB%8C%93%EA%B8%80%20%ED%86%B5%ED%95%A9%20%ED%85%8C%EC%8A%A4%ED%8A%B8/image%203.png)

**4.png**